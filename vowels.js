

const phrase = "kiran is reading a book"
const vowels = "aeiouAEIOU";

for(let i = 0; i < phrase.length; i++)  {
    const currentchar = phrase[i];
    if(vowels.includes(currentchar)){
     console.log(currentchar);
    }
} 





